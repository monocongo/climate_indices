from setuptools import setup

setup(
    name="indices_python",
    version="0.0.1",
    description="Community reference implementations in Python of commonly used climate indices (Palmers, SPI, SPEI, etc.)",
    py_modules=["indices_python"],
    install_requires=[""]
)