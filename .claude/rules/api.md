---
path: src/api/**
---

# REST API Conventions

`src/api/` does not exist in climate_indices v2.4.0. This is a structural placeholder.

If a REST API layer is added in the future, encode conventions here:
- Route naming, HTTP method semantics
- Request/response schemas
- Authentication patterns
- Error response format (should use the `ClimateIndicesError` hierarchy where applicable)
