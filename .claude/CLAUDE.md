# Claude Code — `.claude/` Configuration Directory

This directory contains Claude Code project-layer configuration for `climate_indices`.
For project coding conventions, architecture, and sprint context see `../CLAUDE.md` (root).

## Directory map

| Path | Purpose |
|---|---|
| `rules/architecture.md` | Module boundaries, hard prohibitions, preferred patterns |
| `rules/testing.md` | pytest conventions, TDD policy, numerical assertions |
| `rules/api.md` | REST conventions — path-scoped to `src/api/**` (placeholder) |
| `rules/database.md` | ORM conventions — path-scoped to `src/models/**` (placeholder) |
| `agents/code-reviewer.md` | Subagent: code review against project standards |
| `agents/test-writer.md` | Subagent: TDD-first test writing |
| `commands/review.md` | `/review` — invoke code-reviewer on current diff |
| `commands/ship.md` | `/ship` — lint + typecheck + test + commit |
| `commands/security-scan.md` | `/security-scan` — security audit |
| `tech-spec.md` | Planning artifact for this DX configuration work |
| `CLAUDE.local.md` | Personal overrides template — **gitignored**, never committed |
| `settings.local.json` | Personal settings overrides — **gitignored**, never committed |
| `handovers/` | Session handover files — **gitignored**, auto-generated by PreCompact hook |
