build:
    nodes:
        analysis:
            project_setup:
                override: true
            tests:
                before:
                    - export NUMBA_DISABLE_JIT=1
                override:
                    - pip install -r requirements.txt
                    - python -m unittest tests/test_*.py
filter:
    excluded_paths:
        - compare/*
        - example_inputs/*
        - ingest/*
        - misc/*
        - notebooks/*
        - process/*
        - task/*
        - tests/*
        
checks:
    python:
        code_rating: true
        duplicate_code: true
