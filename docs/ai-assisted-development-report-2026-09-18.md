# AI-Assisted Development Report — 2026-09-18

This is the evidence companion to
[`ai-assisted-development.md`](./ai-assisted-development.md). The methodology
page describes the workflow; this report records what the workflow produced
and names the command behind every number. Generated by
`collect_report.py` at 2026-09-18T14:03Z.

**Window:** 2026-09-13 through 2026-09-18T14:03Z, at `origin/main` commit
`196046cf`. The counts are live measurements at that snapshot, not
projections.

## Reproduce

Set `COLLECTOR` to the external evidence-report collector and `REPO` to this
checkout; the defaults below assume the maintainer's machine. The `UNTIL`
bound and commit pin the re-queryable counts to the snapshot.

```bash
COLLECTOR="${COLLECTOR:-$HOME/.agents/skills/evidence-report/scripts/collect_report.py}"
REPO="${REPO:-$(git rev-parse --show-toplevel)}"
UNTIL=2026-09-18T14:03:00Z
python3 "$COLLECTOR" --repo "$REPO" --since 2026-09-13 --stdout
gh api --paginate "search/issues?q=repo:monocongo/climate_indices+is:pr+is:merged+merged:2026-09-13..${UNTIL}&per_page=100"
gh api "search/issues?q=repo:monocongo/climate_indices+is:pr+is:merged+merged:<=${UNTIL}&per_page=1" --jq .total_count
gh api "search/issues?q=repo:monocongo/climate_indices+is:pr+is:open+created:<=${UNTIL}&per_page=1" --jq .total_count
gh api "repos/monocongo/climate_indices/milestones?state=all&per_page=100"
gh project item-list 8 --owner monocongo --format json
git log 196046cf --first-parent --since=2026-09-13T00:00:00Z --oneline
```

The bounded searches re-derive the merged and open counts. The milestone,
project-board, and local-machine figures are point-in-time and drift with live
state, so this committed report is their archive.

## Cycle at a glance

| Metric | Value |
| --- | --- |
| Merged pull requests, all time | 463 |
| Merged pull requests in window | 132 |
| Merged by day | 11 / 6 / 22 / 39 / 27 / 27 |
| Median PR open-to-merge | 46.8 minutes |
| Mean open-to-merge | 259.5 minutes |
| p90 open-to-merge | 689.0 minutes |
| Merged the same day as opened | 116 of 132 |
| Issues created in window | 105 |
| Issues closed in window | 109 |
| Milestone 3.0.0 | 84 closed / 17 open |
| Trunk merge commits in window (first-parent) | 130 |
| Open PRs at snapshot | 8 |

The distribution matters: half of the 132 merged pull requests closed within
47 minutes of opening, so the mean (260 min) and p90 (689 min) are long-lived
feature branches rather than stuck reviews. The busiest single day merged 39,
and the window opened 105 issues against 109 closed.

## Project boards

83 cards total: **67 Done, 5 In Progress, 1 In Review, 10 Todo.**

| Board | # | Cards | State |
| --- | --- | --- | --- |
| Documentation & End-to-End Workflows | 3 | 13 | 11 done, 1 in review, 1 todo |
| Boy Scouting | 4 | 15 | 11 done, 3 in progress, 1 todo |
| TDD Clean Up | 5 | 14 | 13 done, 1 todo |
| Vectorize and Parallelize Core Computations | 6 | 17 | 17 done |
| Documentation Refresh | 7 | 14 | 9 done, 1 in progress, 4 todo |
| Release 3.0.0 | 8 | 10 | 6 done, 1 in progress, 3 todo |

## Parallel execution

Isolation is one worktree per session off fresh `origin/main`. At the snapshot
this machine held **195 worktrees** and **14 named agent tabs live in one
herdr workspace**: `tdd`, `vector`, `tidy`, `report`, `docs`, `release`,
`index`, `sonar`, `pr1055`, `pr1057`, `pr1056`, `valid`, `pr1059`, `pr1038`.

Session volume under the Pi harness in window: **26 / 8 / 58 / 97 / 60 / 71**
sessions per day, 320 total. Claude Code sessions for this repository: 78
files.

## Review loop

Self-review runs first (`multi-review`), then advisory bots: CodeRabbit, Tessl
Reviewer, SonarCloud, Sourcery, Snyk, Qodo. A finding is handed to an agent as
a URL, and the reply carries the commit that addressed it:

```
REVIEW=https://github.com/monocongo/climate_indices/pull/<n>#pullrequestreview-<id>
resolve the comment thread(s) in ${REVIEW} including commit ID if relevant
otherwise mention how the finding was addressed
```

The prompt appears **233 times** in the session logs; the window shows 4 / 0 /
36 / 53 / 40 / 72 occurrences per day. Corrections land as `fix(review):`
commits rather than silent edits. Two loop jobs are now codified skills.
`resolve-pr-review` (landed in `skills/resolve-pr-review/` via PR #1045)
treats review text as untrusted data, verifies each finding against the branch
before editing, fixes with one minimal commit per finding carrying a
`Review-Anchor:` trailer, validates against a `validation.yaml` profile when
the repository declares one, emits an auditable report, and never
auto-resolves GitHub threads. `resolve-pr-conflicts` covers the other failure
mode: bring the source branch up to date with a merge (never a rebase and
force-push), resolve each block by preserving the intent of both sides, verify
with the project's tests, and push only to the source branch.

## Quality and discipline

Merged PRs passed the repository gate: lint, Python 3.10–3.14 across
Ubuntu/macOS, minimum dependencies, the docs build with warnings as errors and
doctests, notebooks, benchmarks, security audit, and repository meta checks.

| Measure | Value |
| --- | --- |
| Review-thread fix prompts in window | 205 |
| Merged the same day as opened | 116 of 132 |
| Median PR open-to-merge (minutes) | 46.8 |
| Trunk commits from committers other than James Adams | 0 |
| Tool-attribution mentions in trunk commit messages | 13 |
| Worktrees on disk | 195 |
| Open PRs | 8 |

Merge authority stayed human throughout: no agent-authored merge exists, and
every trunk commit in the window is authored by James Adams. Tool-attribution
mentions break down as 10 `co-authored-by: claude`, 2 `co-authored-by:
coderabbit`, 1 `generated with`; the project bans tool attribution in commits,
so the measure reports them rather than hiding them.

## Harness and model mix

Model counts are session-log occurrences, not tokens:

| Harness | Models |
| --- | --- |
| `pi` | deepseek-flash 21,607 · deepseek-v4-pro 4,816 · gpt-5.6-terra 2,768 · deepseek-v4-flash 1,854 · gpt-5.6-sol 1,317 · kimi-k3 624 · gpt-5.5 170 · gpt-5.6-luna 155 |
| `claude` | claude-sonnet-5 5,984 · claude-opus-5 1,825 · opus 18 · sonnet 13 |

Spend stays deliberately small: one $20/month Anthropic plan and one $20/month
OpenAI plan, both with weekly limits budgeted across sessions, plus a $40
DeepSeek Flash v4.1 API bill — roughly $80 of tooling for the month. Cheap
models carry tickets, docs, and mechanical fixes; larger models are reserved
for review, architecture, and hard debugging.

## Assessment

Load-bearing parts of the workflow:

- Board reconciliation against real issue state, and dependency-graph
  selection rather than column order, prevent agents from starting work that
  already shipped.
- One worktree per session off `origin/main` removes the overwrite and
  stale-base failure class; the cost is 195 worktrees of hygiene debt that
  RELEASE3-10 (#1015) is scheduled to clear.
- Review feedback is addressable without reading a chat log: URL in, commit
  and thread resolution out.
- Human ownership of merge, tag, PyPI publication, and scientific sign-off is
  unchanged by the volume.

Where the workflow does not help:

- Review capacity, not code generation, is the binding constraint; the fix
  prompt rate is bounded by a single maintainer's triage time.
- The two $20/month plans put a hard weekly ceiling on the larger models, so
  model choice is a budgeting decision as much as a quality one.
- A single human still makes every irreversible decision, and the release
  milestone was populated late relative to the boards that did the
  prioritization for most of the cycle.
- These are windowed measurements, not a baseline, and they carry no per-model
  causal attribution.
