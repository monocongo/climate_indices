interface:
  display_name: "Climate Indices"
  short_description: "Repo-specific patterns and workflows for climate_indices"
  default_prompt: "Use the climate_indices repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true